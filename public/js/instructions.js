function loadc(q){
	// put the argument on top of the stack
}

function add(){
	// add the top two values on the stack, and replace them with the result
	
}

function print(){
	// send the value on top of the stack to std out
	
}

function halt(){
	// stop processing - disable the execute button
	
}